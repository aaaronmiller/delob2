{
  "name": "@delobotomize/backend",
  "version": "1.0.0",
  "description": "Delobotomize backend API server",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "bun run src/server.ts",
    "build": "tsc",
    "start": "bun run dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@delobotomize/shared": "workspace:*",
    "hono": "^3.11.7"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "bun-types": "^1.0.15",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
